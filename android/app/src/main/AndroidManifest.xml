<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- adjustResize, or the toolbar leaves the screen whenever the
             keyboard opens.

             With no windowSoftInputMode the system chooses, and for a window
             that is one full-screen WebView it chooses to pan: it slides the
             whole window up to keep the caret above the keyboard, carrying the
             controls off the top with it, and nothing in the page can scroll
             them back because from the page's point of view nothing moved.

             The site has `interactive-widget=resizes-content` on its viewport
             meta for the same reason, and that is the right fix in a browser —
             but it is a Chrome setting about Chrome's own viewport, and it has
             no say over how Android resizes this Activity. The app needed its
             own half of the answer. Resizing shrinks the WebView to the room
             above the keyboard, so 100dvh shrinks with it, the app re-lays out,
             and the editor's own scroller puts the caret on screen. -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--
            Keeps audio playing once the screen goes off. An Android WebView is
            suspended in the background unless a foreground service holds the
            process up; the type must be mediaPlayback and must match the type
            passed when the service is started, or Android 14+ refuses to start
            it.
        -->
        <service
            android:name=".MediaNotificationService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Background playback. FOREGROUND_SERVICE_MEDIA_PLAYBACK is required
         from Android 14 for a service of that type; WAKE_LOCK keeps playback
         running rather than being throttled while the device sleeps. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Android 13 shows no notification at all without this, which is how
         playback could keep running with nothing in the shade to control it.
         It used to arrive through a plugin's own manifest; it is declared here
         so it doesn't depend on one. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
